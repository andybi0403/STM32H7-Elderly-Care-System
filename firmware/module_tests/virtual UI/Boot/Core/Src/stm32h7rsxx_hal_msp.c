#include "stm32h7rsxx.h"

/* MSP = MCU Support Package — GPIO peripheral initialization */
/* Currently handled in main.c LED_Init() directly. */
/* This file is reserved for HAL-style MSP callbacks when HAL is added. */
